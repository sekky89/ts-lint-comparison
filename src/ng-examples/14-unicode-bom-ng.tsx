/* ==========================================================================
 * unicode-bom: BOM at file start triggers the rule but causes parse error in TS parser.
 * ========================================================================== */
export const withBom = 1;
